<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
      <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
    </div>
    <div class="modal-body">
      <div class="d-flex justify-content-center my-4">
        <form class=" w-100" action=<%= editMode?`/comments/${comment.id}?_method=PATCH`:'/comments'%>
          method='post'>
          <div class="my-2">
            <label for="username">Username</label>
            <input class="form-control" type="text" name="username" placeholder=<%= editMode? comment.username:''%>>
          </div>
          <div class="my-2">
            <label for="comment">Comment</label>
            <textarea class="form-control" rows="5" name="comment" ><%= editMode? comment.comment:''%></textarea>
          </div>
          <div class="my-2 d-flex justify-content-center">
            <button class="btn btn-outline-primary">Submit</button>
          </div>
        </form>
      </div>

    </div>
  </div>
</div>